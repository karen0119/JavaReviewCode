package com.syntax.review7;

public class BankAccount {

    // instance variables (outside the methods, but inside the class)
    String accountHolder;
    long accountNumber;
    double accountBalance;
    int ssn;

    // static variable (only belongs to the class and methods can access if inside the class)
    static String bankName="BOA";


}
